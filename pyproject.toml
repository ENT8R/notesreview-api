[tool.ruff]
line-length = 79

[tool.ruff.lint]
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
    # isort
    "I",
    # flake8-annotations
    "ANN",
]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "single"
indent-style = "space"
