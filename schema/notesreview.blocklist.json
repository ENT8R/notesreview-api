{
  "$jsonSchema": {
    "bsonType": "object",
    "required": [
      "_id",
      "note",
      "user",
      "hidden_at"
    ],
    "properties": {
      "_id": {
        "bsonType": "objectId",
        "description": "must be an int and is required"
      },
      "note": {
        "bsonType": "int",
        "description": "must be an int and is required"
      },
      "user": {
        "bsonType": "int",
        "description": "must be an int and is required"
      },
      "hidden_at": {
        "bsonType": "date",
        "description": "must be a date and is required"
      }
    }
  }
}
